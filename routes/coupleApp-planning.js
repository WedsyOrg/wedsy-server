/**
 * Couple app — planning endpoints. Mounted from routes/coupleApp.js.
 * Every route: try/catch, section gate, no console.log (hard rules).
 */
const express = require("express");
const router = express.Router({ mergeParams: true });

module.exports = router;
